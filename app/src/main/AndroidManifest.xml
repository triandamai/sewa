<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pmo.sewa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.Error_page"></activity>
        <activity android:name=".pages.user.user_base_page" />
        <activity android:name=".pages.admin.admin_base_page" />
        <activity android:name=".authentication.Auth_completion" />
        <activity android:name=".authentication.Register" />
        <activity android:name=".authentication.Login" />
        <activity android:name=".pages.penyedia.penyedia_base_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>